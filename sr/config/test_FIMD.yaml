# FIMD 测试配置（评估协议与 FIRE test.yaml 保持一致，无 S/P/A 分类）
FIMD:
  data_root: /home/data1/zhangjunhong/sr_project/sr/data/FIMD

PREDICT:
  device: cuda:2

  model_save_path: /home/data1/zhangjunhong/sr_project/sr/res/seed3408_vessel_two_stages/seed3408_vessel_two_stage_03_50_005_0_50/seed3408_vessel_two_stage_ep149_epoch99.pth
  model_type: with_vessel_only_masked

  # FIMD 跨设备/分辨率：refer(r) 缩放到 query(t) 尺寸，GT reference 点同步缩放
  resize_refer_to_query: true

  model_image_width: 768
  model_image_height: 768
  use_matching_trick: True

  # 与 FIRE 相同：按匹配点位移动态选择 homography / quadratic
  registration_method: 'quadratic'
  displacement_threshold: 300.0

  nms_size: 10
  nms_thresh: 0.01
  knn_thresh: 0.9

  use_inverse_consistency: True
  iccl: 3.0

  use_outlier_filter: True
  outlier_criteria: 'homography'
  outlier_threshold: 20.0

  # FIMD 不使用 FIRE 眼区 mask
  eye_mask_path: null

  checkerboard_tile_size: 240
